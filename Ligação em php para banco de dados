//início do Código

<?php

$host = 'localhost';
$user = 'root';
$passwd = '';
$bd_name = 'cad_clientes';

$conx = mysqli_connect($host, $user, $passwd, $bd_name);

//testar conexão depos tirar o comando abaixo

if($conx){
    echo 'Conectou';
}
    else{
        echo 'Erro';
    }

    //Realizar a importação das preferências
    ?>
